#include <stdio.h>
#include <stdlib.h>
#include <time.h>

struct myPets {char * name;};
struct myPets dog;

char* namegen();

struct myPets firstPet();

void reachStats();

void changeName(char *newName);
